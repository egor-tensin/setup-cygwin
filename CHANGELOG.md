v4
--

* x86 is no longer supported (the `platform` parameter was removed).

v3
--

* Added parameters `hardlinks` to convert symlinks in /usr/bin to hardlinks.

v2
--

* Added parameter `env` to configure the CYGWIN environment variable.

v1
--

* Initial release.
